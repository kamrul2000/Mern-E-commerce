import React from 'react';
import Navbar from '../Shared/Navbar/Navbar';

const Profile = () => {
    return (
        <div>
            <Navbar/>
            Profile page
        </div>
    );
};

export default Profile;